\chapter{Introduction}
\label{chap:intro}

\paragraph{The problem.} Identifying an object from any perspective is a fundamental capability for intelligent vision systems that operate with multiple cameras. \textit{Cross-view object correspondence} aims at matching objects across complementary viewpoints and underpins a growing class of real-world applications. In human-robot collaboration, an aerial vehicle and a ground-based human operator must agree on which object in the scene is being referenced, despite observing it from entirely different angles \cite{collab_1, collab_2}. Augmented reality assistants face the same requirement; a camera mounted on the head captures the user's immediate environment, while a fixed camera provides broader context of the scene \cite{wearable}. The growing adoption of consumer smart glasses means that these capabilities can be extended to everyday personal use, for instance an application that helps a user find an object in his/her home by relating what the glasses see with the feed of a house camera. Another application is multi-robot manipulation, where independent agents with non-overlapping fields of view must coordinate around shared objects without access to a common reference frame \cite{multirobot_1, multirobot_2}. In each of these domains, failure is costly, since a system that cannot identify the same object across views cannot collaborate, cannot respond to instruction, and cannot be deployed safely. Despite steady progress in object segmentation on single images, cross-view segmentation in video remains an open challenge.

\paragraph{The dataset.} The most comprehensive public resource for studying this problem is \textit{Ego-Exo4D}  \cite{grauman2024egoexo4d}. It holds thousands of video recordings of human activities captured simultaneously from a first-person
\textit{egocentric} camera worn by the participant and one or more \textit{exocentric} cameras fixed around the scene. The dataset enables many novel computer vision tasks, including cross-view object correspondence: given a per-frame segmentation mask track for an object in one view, predict the mask track for the same object in the other view. The task is particularly demanding because the two perspectives focus on different aspects of the same scene. On the one hand, the egocentric view captures object detail and manual interactions with high fidelity, but suffers from motion blur, frequent hand occlusion, and a narrow field of view that loses scene context. On the other hand, the exocentric view provides a global picture of the environment and the person's body, but sacrifices detailed resolution for each individual object. As a result, the same object appears in the two views at drastically different scales, angles, positions in the frame, and even colour brightness due to differing lighting conditions. Moreover, the cameras lack geometric calibration information, which renders triangulation-based approaches unfeasible. Finally, the wide variety of recorded scenes, camera optics, and imaging conditions introduces a broad domain shift range that challenges any method developed for a fixed distribution. \cref{fig:qualitative} shows qualitative examples for both correspondence directions.
\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{latex/imgs/ch2/qualitative-examples.png}
    \caption{Qualitative examples of cross-view object correspondence for both \textit{Ego2Exo} (above) and \textit{Exo2Ego} (below) directions. \textcolor{blue}{Blue} is the ground truth mask; \textcolor{green}{green}, \textcolor{yellow}{yellow}, \textcolor{orange}{orange} are top 3 mask predictions generated by Our Pipeline.}
    \label{fig:qualitative}
\end{figure}

\paragraph{Existing solutions.} Current state-of-the-art (SOTA) methods address this challenge by representing objects with embeddings and then learning how to match them across views. To date, Hu et al.\ (2025) \cite{hu2025lmeec} achieve the strongest published results by adapting SAM~2 with a Mixture-of-Experts fusion module and a dual compressed long-term memory system. O-MaMa \cite{o-mama}, the winner of the 2025 Ego-Exo4D Correspondences Challenge, generates candidate
masks in the destination view via FastSAM and selects the best match using pooled DINOv2features in a learned cross-view latent space. \cite{pan2025v2sam} take a
complementary route, adapting SAM~2 with dual prompt generators and a cyclic-consistency selector. All related works will be explored in depth in \cref{chap:theory}. However, these approaches share two crucial limitations. First, they rely on training pipelines, which ties each model to the distribution of available training data, making them inapplicable to novel camera configurations or scenarios without retraining. Second, they lack interpretability, as only attention maps can be used to inspect why a particular object was segmented in a particular frame, hindering their applicability in the real world. \cite{xai-sensors}.

\paragraph{Foundation models.} Over the last five years, a new paradigm in machine learning emerged: foundation models. The emergence of large language models has demonstrated across a wide range of settings that systems trained on broad data and used without task-specific fine-tuning can match or exceed specialist \cite{wu2023exploringtradeoffsunifiedlarge, kim2023languagemodelssolvecomputer,
gruver2024largelanguagemodelszeroshot}. A similar transition is happening in computer vision. Currently, most problems are addressed by designing task-specific architectures and collecting task-specific supervised data. In contrast, LeCun and others
\cite{maes2026leworldmodelstableendtoendjointembedding, assran2025vjepa2selfsupervisedvideo, dinov1}
argue self-supervised learning on video data is the most promising path towards building models that understand the word dynamics at a truly foundational level. While text is already an abstract compression of information, video data inherently captures information about physics, object permanence, causality, and the consequences of action. Therefore, if a model has internalized how the world works, it develops the kind of competence that should transfer across tasks and modalities.

Our work is inspired by this philosophy: if foundation models with vision capabilities have acquired a
sufficiently general understanding of the visual world, carefully composing them at inference
time should transfer to novel tasks that none was designed for.

\paragraph{Language as a bridge.} These observations converge on the question this thesis sets out to answer. We ask whether natural language can serve as a bridge between machine learning architectures, acting as an information-compression medium that transfers a scene from one model to another. The question is compelling for two reasons. First, it mirrors how humans collaborate: we perceive a scene, compress what we see into language, which apart from mathematics is the most compressed information-transfer mechanism available to us, and rely on that compression to coordinate with others who observe the same scene from a different vantage point. Second, a system built on this principle is interpretable by construction, because a pipeline whose modules communicate through a human-readable phrase exposes its own reasoning, so that any prediction can be inspected, and any unsafe or erroneous output traced, without recourse to model weights. Cross-view object correspondence on \textit{Ego-Exo4D} furnishes a concrete instance of this question, since the two views are two perspectives on a single scene and language must carry object identity from one to the other. A secondary motivation follows from the same setup. Because no module is trained for the task, the pipeline additionally tests whether independent foundation models can collaborate on a problem none was designed to solve.
\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{latex/imgs/ch2/pipeline-overview.png}
    \caption{The four stages of the proposed pipeline.}
    \label{fig:pipeline-summary}
\end{figure}
\paragraph{Our Pipeline.} To this end, we propose the ``two eyes and one mouth'' pipeline. Two cameras observe the scene from complementary perspectives, and a single natural language description mediates the object correspondence between them, illustrated in \cref{fig:ego-exo-example}. Given a source-view mask track, we first select the single highest-quality seed frame from the source view and pass it to a visual large language model, which generates a view- and time- independent description of the target object. That description then drives two coordinated processes. First, an open-vocabulary detector scores destination frames by object-text alignment to select the most informative anchor frames. Second, a SAM~3 agentic loop localises and segments the object in each anchor frame using the same description as the grounding signal. Finally, SAM~2's video tracker completes the mask track across all remaining destination frames by propagating predictions conditioned on a memory bank of anchor and recently processed frames.

Crucially, no component of the pipeline is fine-tuned for the task. Because it inherits no dependence on the distribution of any cross-view dataset, the pipeline is in principle both camera and dataset agnostic, meaning that it can be applied to novel camera configurations and scenarios without retraining, directly addressing the first limitation of existing solutions. 
The bridge between views is a human-readable JSON description, and any erroneous prediction can be localised to the specific block whose output is at fault.

\textcolor{red}{Our pipeline achieves $37.7$ (\textit{Ego2Exo}) and $40.6$ (\textit{Exo2Ego}) IoU on the
\textit{Ego-Exo4D} Correspondences v2 test split, representing a relative gain of $9\%$ and
$62\%$ against the official challenge baselines respectively.} These results place our approach within a few percentage points of O-MaMa \cite{o-mama}, the 2025 challenge winner, without any cross-view supervision. 

\paragraph{Thesis overview.} We begin this dissertation in \cref{chap:theory} with establishing the theoretical foundations that underpin our pipeline, covering the architectures for visual representation, segmentation, language grounding, video propagation, a light review of large language models, and a recap of related methods for cross-view object correspondence.  Next, the two biggest contributions. In \hyperref[chap:pipeline]{Chapter~4}, we describe the full pipeline and the experimental set-up, and in \hyperref[chap:experiments]{Chapter~5}, we present the results, an ablation study, and a detailed investigation of the performance of the pipeline. We deliberatively outline the gains but focus mostly on the failure modes of the pipeline to study how foundation models behave over a task none of them was designed for, making this thesis an interpretability contribution relevant for future works. 